import AppConfig from './app.config';
import DocConfig from './doc.config';
import JwtConfig from './jwt.config';
import SecurityConfig from './security.config';

export default [AppConfig, DocConfig, SecurityConfig, JwtConfig];
